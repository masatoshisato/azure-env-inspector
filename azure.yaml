# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-env-inspector
metadata:
  template: azure-env-inspector@0.0.1-beta

services:
  app:
    project: .
    language: ts
    host: containerapp

infra:
  provider: bicep
  path: infra
