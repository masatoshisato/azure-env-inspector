# apps/env-inspector フォルダー詳細説明書

作成日: 2026年1月21日

## フォルダーの目的

`apps/env-inspector/` は、Azure環境の動作確認・構成可視化ツールのアプリケーションコードを格納するフォルダーです。TypeScriptで実装されたNode.jsアプリケーションで、Azure上の各種リソース（PostgreSQL、Blob Storage、Queue Storage、Key Vaultなど）への接続確認と動作テストを行います。

---

## ファイル一覧と目的

### 1. `test.ts`
**目的**: アプリケーションのメインエントリーポイント

**内容の詳細説明**:
```typescript
// HTTPサーバーモジュールのインポート
import * as http from 'http';

// HTTPサーバーの作成
// - リクエストを受けると200 OKとテキストレスポンスを返す
const server = http.createServer((req, res) => {
  res.statusCode = 200;                        // HTTPステータスコード200（成功）
  res.setHeader('Content-Type', 'text/plain'); // レスポンスヘッダー設定
  res.end('Hello, World!\n');                  // レスポンスボディ
});

// ポート番号の定義
const PORT = 3000;

// サーバーをポート3000で起動
server.listen(PORT, () => {
  console.log(`Server running at http://localhost:${PORT}/`);
});

// テスト目的でサーバーオブジェクトをエクスポート
export default server;
```

**技術詳細**:
- Node.jsの標準`http`モジュールを使用した基本的なWebサーバー
- 現状はシンプルな"Hello, World"を返すのみ
- 今後、Azure各種リソースへの接続確認機能を追加予定

---

### 2. `package.json`
**目的**: Node.jsプロジェクトの設定ファイル

**内容の詳細説明**:
```json
{
  "name": "azure-env-inspector",          // パッケージ名
  "version": "1.0.0",                     // バージョン番号
  "description": "",                      // プロジェクトの説明（現在空）
  "main": "index.js",                     // エントリーポイント（未使用）
  
  // npm scriptsの定義
  "scripts": {
    "dev": "tsx watch test.ts",           // 開発モード（ファイル変更を監視して自動再起動）
    "start": "tsx test.ts",               // 本番実行（単発実行）
    "test": "echo \"Error: no test specified\" && exit 1"  // テスト（未実装）
  },
  
  "keywords": [],                         // 検索キーワード
  "author": "",                           // 作者情報
  "license": "ISC",                       // ライセンス
  
  // 開発時の依存パッケージ
  "devDependencies": {
    "@types/node": "^25.0.3",             // Node.js型定義ファイル
    "tsx": "^4.19.2"                      // TypeScript実行ツール
  }
}
```

**各パッケージの役割**:

| パッケージ | 役割 |
|-----------|------|
| `@types/node` | Node.jsの型定義。TypeScriptでNode.js標準モジュールを使用する際に必要 |
| `tsx` | TypeScriptを直接実行できるツール。トランスパイル不要で開発効率向上 |

---

### 3. `package-lock.json`
**目的**: 依存パッケージのバージョン固定

**技術詳細**:
- `npm install`実行時に自動生成されるファイル
- 依存パッケージの正確なバージョンとダウンロードURLを記録
- チームメンバー間で同じバージョンのパッケージを使用することを保証
- 手動編集は不要（npmが自動管理）

**内容**（574行、主要部分のみ抜粋）:
- ロックファイルバージョン: `3`
- Node.jsパッケージ: `@types/node@25.0.3`、`tsx@4.19.2` とその依存関係
- 整合性チェック用のハッシュ値を含む

---

## アプリケーションのビルド方法

### 現状のビルド方法

**このプロジェクトはビルド不要です**

理由:
- TypeScriptを`tsx`で直接実行するため、トランスパイル（TS→JS変換）が不要
- 開発効率を優先した構成

### 将来的にビルドが必要になる場合

本番環境にデプロイする際は、以下の手順を推奨:

```bash
# TypeScriptコンパイラーのインストール
npm install --save-dev typescript

# tsconfig.jsonの作成
npx tsc --init

# ビルド実行
npx tsc

# 生成されたJSファイルを実行
node dist/test.js
```

**package.jsonへの追加スクリプト例**:
```json
"scripts": {
  "build": "tsc",
  "start:prod": "node dist/test.js"
}
```

---

## アプリケーションのデバッグ方法

### 方法1: VS Codeデバッガーを使用（推奨）

#### 前提条件
- VS CodeまたはGitHub Codespaces上で開発している
- `.vscode/launch.json`が設定済み（本プロジェクトでは既に設定済み）

#### デバッグ手順

1. **ブレークポイントの設定**
   ```
   apps/env-inspector/test.ts を開く
   → 行番号の左側をクリックして赤い丸を表示
   → 例: 5行目（`res.statusCode = 200;`）にブレークポイント
   ```

2. **デバッグパネルを開く**
   ```
   左サイドバーの「実行とデバッグ」アイコン（▶️🐛）をクリック
   または Ctrl+Shift+D (Mac: Cmd+Shift+D)
   ```

3. **デバッグ構成を選択**
   ```
   ドロップダウンメニューから「Debug test.ts」を選択
   ```

4. **デバッグ開始**
   ```
   緑の▶️ボタンをクリック
   または F5 キー
   ```

5. **デバッグ操作**
   - **F10**: ステップオーバー（次の行へ）
   - **F11**: ステップイン（関数内に入る）
   - **Shift+F11**: ステップアウト（関数から出る）
   - **F5**: 続行（次のブレークポイントまで）
   - **Shift+F5**: 停止

6. **変数の確認**
   ```
   左サイドバーの「変数」パネルで現在のスコープの変数を確認
   マウスオーバーでも変数の値を確認可能
   ```

7. **動作確認**
   ```bash
   # 別のターミナルで実行
   curl http://localhost:3000/
   
   # または GitHub Codespacesの場合
   「ポート」タブから「ブラウザで開く」をクリック
   ```

#### GitHub Codespacesでの特記事項

- ポート3000が自動的にフォワーディングされる
- 右下に「ポート3000が開いています」通知が表示される場合がある
- 「ブラウザで開く」をクリックすると外部からアクセス可能

---

### 方法2: ターミナルから実行

#### 通常実行
```bash
cd apps/env-inspector
npm start
```

**実行結果**:
```
> azure-env-inspector@1.0.0 start
> tsx test.ts

Server running at http://localhost:3000/
```

#### 開発モード（ホットリロード）
```bash
cd apps/env-inspector
npm run dev
```

**特徴**:
- ファイル変更を監視
- 変更を保存すると自動的にサーバーが再起動
- 開発効率が向上

**実行結果**:
```
> azure-env-inspector@1.0.0 dev
> tsx watch test.ts

Server running at http://localhost:3000/
```

#### 動作確認
```bash
# 別のターミナルで実行
curl http://localhost:3000/

# 出力
Hello, World!
```

---

### 方法3: console.logデバッグ

簡易的なデバッグ方法:

```typescript
// test.ts の例
const server = http.createServer((req, res) => {
  console.log('リクエスト受信:', req.method, req.url);  // 追加
  res.statusCode = 200;
  res.setHeader('Content-Type', 'text/plain');
  res.end('Hello, World!\n');
});
```

実行後、ターミナルにログが出力されます。

---

## トラブルシューティング

### エラー: `Cannot find module 'tsx'`

**原因**: 依存パッケージがインストールされていない

**解決方法**:
```bash
cd apps/env-inspector
npm install
```

---

### エラー: `Port 3000 is already in use`

**原因**: ポート3000が既に使用されている

**解決方法1**: 既存プロセスを停止
```bash
# プロセスIDを確認
lsof -i :3000

# プロセスを停止
kill -9 <PID>
```

**解決方法2**: 別のポートを使用
```typescript
// test.ts を編集
const PORT = 3001;  // ポート番号変更
```

---

### デバッグが開始されない

**原因**: launch.jsonのパスが正しくない

**解決方法**: `.vscode/launch.json` を確認
```json
{
  "program": "${workspaceFolder}/apps/env-inspector/test.ts",
  "runtimeExecutable": "${workspaceFolder}/apps/env-inspector/node_modules/.bin/tsx",
  "cwd": "${workspaceFolder}/apps/env-inspector"
}
```

すべてのパスが正しいことを確認してください。

---

## 今後の拡張予定

### Azure リソース接続機能の追加

1. **PostgreSQL接続確認**
   ```typescript
   import { Client } from 'pg';
   // PostgreSQLへの接続とクエリ実行
   ```

2. **Blob Storage接続確認**
   ```typescript
   import { BlobServiceClient } from '@azure/storage-blob';
   // Blobのアップロード・ダウンロード
   ```

3. **Key Vault接続確認**
   ```typescript
   import { SecretClient } from '@azure/keyvault-secrets';
   import { DefaultAzureCredential } from '@azure/identity';
   // シークレットの取得
   ```

4. **Queue Storage接続確認**
   ```typescript
   import { QueueServiceClient } from '@azure/storage-queue';
   // メッセージの送受信
   ```

### 環境変数の管理

`.env`ファイルを使用した設定管理:

```bash
# .env ファイル例
PORT=3000
POSTGRES_HOST=your-server.postgres.database.azure.com
POSTGRES_DATABASE=appdb
KEY_VAULT_URL=https://your-keyvault.vault.azure.net/
```

```typescript
// dotenv パッケージで読み込み
import * as dotenv from 'dotenv';
dotenv.config();

const PORT = process.env.PORT || 3000;
```

---

## まとめ

- **現状**: シンプルなHTTPサーバー（Hello World）
- **開発ツール**: tsx（TypeScript直接実行）
- **デバッグ**: VS Codeデバッガー対応済み
- **実行方法**: `npm start`（本番）、`npm run dev`（開発）
- **今後**: Azure各種リソースへの接続確認機能を追加予定
